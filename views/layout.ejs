<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= typeof title !== 'undefined' ? title : 'TV Tracker' %></title>
<link rel="stylesheet" href="/style.css">
</head>
<body>
<header>
  <div class="header-left">
    <a class="btn secondary" href="https://home-hub.dynamicdexter.com/">🏠 Home Hub</a>
  </div>

  <div class="header-center">
    <a class="brand" href="/">📺 TV Tracker</a>
  </div>

  <div class="header-right">
    <% if (user) { %>
      <span class="muted" style="margin-right:8px;">Signed in as <strong><%= user.username %></strong></span>
      <form method="post" action="/auth/logout" style="display:inline;">
        <button class="btn secondary" type="submit">Log out</button>
      </form>
    <% } else { %>
      <a class="btn secondary" href="/auth/login">Log in</a>
    <% } %>
    <a class="btn" href="/add" style="margin-left:8px;">+ Add show</a>
  </div>
</header>
<main>
<%- body %>
</main>
<script src="/app.js"></script>
</body>
</html>
